# Core dependencies with latest stable versions
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22
pydantic==2.12.5

# MarkItDown with all optional dependencies for comprehensive document support
# Includes: docx, pptx, xlsx, xls, pdf, outlook, az-doc-intel, audio-transcription, youtube-transcription
markitdown[all]>=0.1.4

# LLM support (OpenAI and Azure OpenAI) - for custom LLM client configuration
openai>=1.0.0

# Azure identity - for custom credential handling
azure-identity>=1.15.0

# Optional: Rate limiting (install with: pip install slowapi)
# slowapi==0.1.9

# Security pins — force upgrades of transitive deps flagged by Snyk
anyio>=4.4.0
certifi>=2024.7.4
h11>=0.16.0
idna>=3.7
jinja2>=3.1.6
orjson>=3.11.6
pygments>=2.20.0
starlette>=0.49.1
ujson>=5.12.0
# onnxruntime>=1.24.1  # TODO: pin once fix version is published (Snyk SNYK-PYTHON-ONNXRUNTIME-15869956)
