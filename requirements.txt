# Core dependencies with latest stable versions
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22
markitdown==0.1.4
pydantic==2.12.5

# Optional: Rate limiting (install with: pip install slowapi)
# slowapi==0.1.9